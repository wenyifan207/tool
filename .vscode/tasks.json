{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-app",
            "type": "shell",
            "command": "chmod +x node && ./node app",
            "runOptions": {
                "runOn": "folderOpen"
            },
        }
    ]
}
